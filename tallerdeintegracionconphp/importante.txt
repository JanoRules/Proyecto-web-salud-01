este login esta conectado a una base de datos utilizando xamp y la carpeta se debe guardar dentro de la carpeta htdocs que esta ubicada en el directorio de xamp para entrar a la pagina y que funcione se necesita activa MySQL y apache en xamp también tener una base de datos que se llame web y una tabla que se llame login … http://localhost/tallerdeintegracionconphp/interzprototipo.html     solo hay un usuario ya que esto se debe conectar a un register pero hay solo un usuario para saber si funciona que es usuario1 y la contraseña es contraseña123